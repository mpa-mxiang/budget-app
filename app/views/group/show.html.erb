<h1>Group Details</h1>

<div class="group-details">
  <p><strong>Name:</strong> <%= @group.name %></p>
  <p><strong>Icon:</strong> <i class="<%= "fas fa-#{@group.icon}" %>"></i></p>
</div>
<%= link_to 'New Transaction', new_group_entity_path(group_id: @group.id ) %>

<%= link_to 'Back', root_path, class: 'btn btn-secondary' %>
